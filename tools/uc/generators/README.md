# UC Generators

Generators keep canonical metadata and derived artifacts aligned.

- `generate_from_contract.py` regenerates DAG config from the canonical contract.

Normal edit flow:

```powershell
py -3 tools\uc\generators\generate_from_contract.py
```
