version: 2
models:
  - name: dim_delivery_resource
    description: "dimension for delivery_resource"
    columns:
      - name: id
        tests: [not_null]
      - name: fullname
        tests: [not_null]
      - name: delivery_type_id
        tests: [not_null]
      - name: store_id
        tests: [not_null]
      - name: active_flag
        tests: [not_null]
      - name: _ingested_at
