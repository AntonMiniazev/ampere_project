version: 2
models:
  - name: dim_order_statuses
    description: "dimension for order_statuses"
    columns:
      - name: id
        tests: [not_null]
      - name: order_status
        tests: [not_null]
      - name: _ingested_at
